-- nvim-tree: 파일 탐색기를 사이드바에 표시해주는 플러그인
return {
  "nvim-tree/nvim-tree.lua",
  version = "*",
  -- 키 매핑은 keymaps.lua에서 관리
  lazy = false,
  dependencies = {
    "nvim-tree/nvim-web-devicons",
  },
  config = function()
    require("nvim-tree").setup {}
  end,
}